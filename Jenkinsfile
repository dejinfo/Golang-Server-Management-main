pipeline {
    agent any 
    stage('build') {
            steps{
            
            sh 'docker  build'
            echo 'docker images build'
            }
        }
        stage('test') {
            steps{

            sh 'docker build'
            echo 'docker images build'
            }
        }

}
}

pipeline {
    agent any 
    stage('build') {
            steps{
            
            sh 'docker-compose.yml'
            echo 'docker-compose.yml'
            }
        }
        stage('test') {
            steps{

            sh 'docker-compose.yml'
            echo 'docker-compose.yml'
            }
        }

}

